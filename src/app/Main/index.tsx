import { FC } from 'react';

import { Navbar } from '@/shared/ui';

export const MainPage: FC = () => {
    return (
        <div>
            <Navbar />
            Main
        </div>
    );
};

export default MainPage;
